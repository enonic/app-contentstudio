import type { ReactElement } from 'react';
import { BrowseAppShell } from './BrowseAppShell';

export const BrowsePage = (): ReactElement => {
    return <BrowseAppShell />;
};

BrowsePage.displayName = 'BrowsePage';
